{
  "name": "deploy-firestore-rules",
  "version": "1.0.0",
  "description": "Script para fazer deploy das regras do Firestore",
  "scripts": {
    "deploy-rules": "firebase deploy --only firestore:rules",
    "deploy-indexes": "firebase deploy --only firestore:indexes"
  }
}
